const {
  PATH_LAYOUT_BASIC,
  PATH_HOME,
  PATH_PRODUCTS_ALL,
} = require("./pathFiles");
const { ID_PAGE_HOME } = require("./views");

module.exports = {
  PATH_HOME,
  PATH_LAYOUT_BASIC,
  ID_PAGE_HOME,
  PATH_PRODUCTS_ALL,
};
